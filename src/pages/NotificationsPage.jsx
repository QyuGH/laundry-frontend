/**
 * Notifications page — content pending.
 * Displays user notifications with mark-as-read functionality.
 *
 * @returns {JSX.Element}
 */
function NotificationsPage() {
  return (
    <div className="p-6">
      <p className="text-white/40 text-sm">Notifications</p>
    </div>
  );
}

export default NotificationsPage;
