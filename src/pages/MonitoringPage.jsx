/**
 * Monitoring page — content pending.
 * Displays live session controls, drying progress,
 * sensor readings, and scheduling panel.
 *
 * @returns {JSX.Element}
 */
function MonitoringPage() {
  return (
    <div className="p-6">
      <p className="text-white/40 text-sm">Monitoring</p>
    </div>
  );
}

export default MonitoringPage;
