/**
 * Activity Log page — content pending.
 * Displays a chronological list of device events.
 *
 * @returns {JSX.Element}
 */
function ActivityLogPage() {
  return (
    <div className="p-6">
      <p className="text-white/40 text-sm">Activity Log</p>
    </div>
  );
}

export default ActivityLogPage;
