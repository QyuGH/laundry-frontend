/**
 * Account Settings page — content pending.
 * Displays device configuration, location settings,
 * and member management (owner only).
 *
 * @returns {JSX.Element}
 */
function SettingsPage() {
  return (
    <div className="p-6">
      <p className="text-white/40 text-sm">Account Settings</p>
    </div>
  );
}

export default SettingsPage;
